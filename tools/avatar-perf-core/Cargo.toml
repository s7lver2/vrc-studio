[package]
name = "avatar-perf-core"
version = "1.0.0"
edition = "2021"

[[bin]]
name = "avatar-perf-core"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"
walkdir = "2"
chrono = { version = "0.4", features = ["serde"] }
